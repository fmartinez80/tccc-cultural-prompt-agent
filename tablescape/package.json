{
  "name": "tablescape",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test build: SceneSpec -> rule-based layout -> headless three.js proxy render",
  "scripts": {
    "render": "node src/run.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
